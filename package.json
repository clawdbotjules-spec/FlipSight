{
  "name": "flipsight",
  "version": "0.1.0",
  "private": true,
  "description": "Real-time marketplace arbitrage engine — monitors marketplaces and clearance feeds, values items against sold-price data, and pushes instant deal alerts.",
  "type": "module",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "npm run build -w @flipsight/shared && npm run build -w @flipsight/db && npm run build -w @flipsight/worker-core && npm run build -w @flipsight/api && npm run build -w @flipsight/worker-ebay && npm run build -w @flipsight/worker-keepa && npm run build -w @flipsight/worker-goodwill && npm run build -w @flipsight/worker-retail && npm run build -w @flipsight/worker-estate",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "test": "npm run test -w @flipsight/shared",
    "dev:api": "npm run dev -w @flipsight/api",
    "db:generate": "prisma generate --schema packages/db/prisma/schema.prisma",
    "db:migrate": "prisma migrate dev --schema packages/db/prisma/schema.prisma",
    "db:deploy": "prisma migrate deploy --schema packages/db/prisma/schema.prisma",
    "db:seed": "tsx packages/db/src/seed.ts",
    "seed:deal": "tsx packages/db/src/seed-deal.ts",
    "smoke": "node scripts/smoke.mjs",
    "listen": "node scripts/listen.mjs"
  },
  "devDependencies": {
    "tsx": "^4.23.11",
    "typescript": "^7.0.2",
    "ws": "^8.21.3"
  }
}
